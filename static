/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/your-repo-name', // استبدل your-repo-name باسم المستودع الخاص بك
}

module.exports = nextConfig
